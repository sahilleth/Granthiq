from fastapi import APIRouter

# Placeholder router - can be implemented later for document sources management
# Note: Document sources are currently managed via /documents endpoints
# This router can be used for source templates, web scraping sources, etc.
router = APIRouter(prefix="/sources", tags=["sources"])
