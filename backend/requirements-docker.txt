# =============================================================================
# OPTIMIZED DOCKER REQUIREMENTS
# Targeted for Railway/Render/Docker environments (Linux/CPU)
# =============================================================================

# 🔥 CRITICAL: Use CPU-only PyTorch wheels (saves ~2GB of image size!)
--extra-index-url https://download.pytorch.org/whl/cpu

# =====================
# 1. Base AI Frameworks (Install First)
# =====================
# Pinned to CPU versions to prevent GPU bloat
torch>=2.2.0
# Transformers (HuggingFace)
transformers==4.41.2
sentence-transformers==3.0.1
huggingface-hub>=0.23.0,<1.0

# =====================
# 2. Core Web API
# =====================
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0
slowapi==0.1.9

# =====================
# 3. Database & Task Queue
# =====================
sqlmodel==0.0.19
sqlalchemy==2.0.30
# AsyncPG for SQLAlchemy async engine
asyncpg==0.29.0
# Psycopg (v3) for Procrastinate (Task Queue) - Binary avoids build tools
psycopg[binary]>=3.1.19
psycopg-pool>=3.1.0  # 🔥 CRITICAL: Required by procrastinate for connection pooling
# Procrastinate Task Queue
procrastinate[psycopg]>=2.5.0
# Database Migrations
alembic==1.13.1
# Supabase Client (Storage/Auth)
supabase==2.5.0

# =====================
# 4. LlamaIndex (RAG Core)
# =====================
# llama-index-core 0.11+ requires native Pydantic v2 - all sub-packages must be compatible
llama-index-core>=0.11.0
llama-index-vector-stores-qdrant>=0.6.0  # Must be >=0.4.0 for llama-index-core>=0.11.0 Pydantic v2 compat
llama-index-embeddings-huggingface>=0.3.1
llama-index-embeddings-openai>=0.2.2
llama-index-llms-openai>=0.1.27
llama-index-llms-groq>=0.1.3
llama-index-llms-gemini>=0.1.6
llama-index-llms-litellm>=0.6.3  # 🔥 CRITICAL: Required by src/services/llm/factory.py
llama-index-postprocessor-cohere-rerank>=0.1.6
llama-index-readers-file>=0.1.23
fastembed>=0.3.1  # 🔥 CRITICAL: Required for BM42 sparse vectors in Qdrant

# =====================
# 5. LangChain & RAGAS (Evaluation)
# =====================
# RAGAS 0.4.0 required for aevaluate async API
ragas==0.4.0
# LangChain ecosystem - updated to match pyproject.toml
langchain-core>=1.1.0
langchain-google-genai>=3.2.0
langchain-groq>=1.1.0
langchain-huggingface>=1.2.0
langchain-litellm>=0.3.5
langchain-openai>=1.1.0
langchain-community>=0.3.0  # Compatible with langchain-core 1.x
litellm>=1.40.0
datasets==2.19.1

# =====================
# 6. Observability
# =====================
langfuse>=2.33.0
openinference-instrumentation-llama-index>=1.0.0
sentry-sdk>=2.48.0

# =====================
# 7. Document Processing
# =====================
pypdf>=5.0.0
beautifulsoup4==4.12.3
# Unstructured (Basic text extraction) - with PDF, docx, xlsx, pptx, images, csv, md support
unstructured[all-docs]>=0.18.15
pdfminer.six>=20231228
python-magic==0.4.27
pyyaml==6.0.2
striprtf==0.0.26
numpy>=1.24.0,<2.4

# =====================
# 8. Audio & Video
# =====================
# Text-to-Speech
edge-tts>=6.1.12
google-cloud-texttospeech>=2.14.1
elevenlabs==1.11.0     # TTS API
# Audio processing
pydub==0.25.1
soundfile==0.12.1
mutagen==1.47.0        # Audio metadata
# YouTube
yt-dlp>=2024.04.09
youtube-transcript-api==0.6.2
yt-dlp-getpot-wpc>=1.0.0
# Transcription
assemblyai==0.26.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.189.0
google-auth>=2.45.0
google-auth-httplib2>=0.3.0

# =====================
# 9. Web Scraping & Parsing
# =====================
firecrawl>=4.5.0       # Web scraping service

# =====================
# 10. LlamaParse (Document Parsing)
# =====================
llama-parse>=0.4.0
llama-index-readers-llama-parse>=0.1.0
llamaindex-py-client>=0.1.0

# =====================
# 11. HTTP Clients & Networking
# =====================
requests==2.32.3
httpx==0.27.0
aiohttp>=3.9.0
httptools==0.7.1
# HTTP/2 Support
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0
# Network utilities
yarl>=1.0.0
multidict>=6.0.0
frozenlist>=1.0.0
aiosignal>=1.0.0

# =====================
# 12. Data Processing
# =====================
dill==0.4.0
multiprocess>=0.70.0

# =====================
# 13. Async Utilities
# =====================
aiofiles==23.2.1
nest-asyncio==1.6.0

# =====================
# 14. Utilities & Fixes
# =====================
loguru>=0.7.2
# 🔥 CRITICAL: Pin tenacity to 8.3.0 to fix LlamaIndex vs LangChain conflict
tenacity==8.3.0  
tqdm==4.66.4

# JSON & Data
dirtyjson==1.0.8
jsonpath-python==1.0.6
eval-type-backport==0.2.2

# Compression & Encoding
brotli==1.1.0
chardet==5.2.0

# System
appdirs==1.4.4
portalocker==2.10.1
attrs>=23.0.0
greenlet>=3.0.0         # Required by SQLAlchemy

# Build/Config
pluggy==1.6.0
iniconfig==2.3.0
grpcio-tools>=1.50.0

# Testing
pytest==8.2.1
pytest-asyncio==0.23.7

# =============================================================================
# NOTES:
# =============================================================================
# ✅ langchain-litellm + litellm: Required by src/evaluation/ragas_evaluator.py
# ✅ langchain-huggingface: Required for HuggingFaceEmbeddings fallback
# ✅ tenacity==8.3.0: Prevents conflict between llama-index and langchain
# ✅ CPU-only torch: Saves 2GB by avoiding nvidia-cuda packages
# ✅ llama-parse: Document parsing service
# ✅ kokoro: Local TTS engine (no API key)
# ✅ firecrawl: Web scraping
# 
# Total packages: ~88
# Image size: ~1.2-1.5 GB (CPU-only)
# Build time: ~3-4 minutes
# =============================================================================
